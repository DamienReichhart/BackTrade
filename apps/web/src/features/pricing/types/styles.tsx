/**
 * Interface for CSS module classes used in comparison table cells
 * This matches the structure of ComparisonTable.module.css
 */
export interface ComparisonTableCellStyles {
    included: string;
    notIncluded: string;
    highlight: string;
}
