apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
    name: allow-backend-to-redis
    namespace: backtrade
spec:
    podSelector:
        matchLabels:
            app: redis
            component: cache
    policyTypes:
        - Ingress
    ingress:
        - from:
              - podSelector:
                    matchLabels:
                        app: api
                        component: backend
          ports:
              - protocol: TCP
                port: 6379
