name: Lint
description: Run ESLint across the monorepo using pnpm
inputs:
  working-directory:
    description: Directory to run the command in
    required: false
    default: "."
runs:
  using: composite
  steps:
    - name: Lint
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: pnpm lint
