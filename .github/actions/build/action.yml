name: Build
description: Build all apps in the monorepo via pnpm
inputs:
  working-directory:
    description: Directory to run the command in
    required: false
    default: "."
runs:
  using: composite
  steps:
    - name: Build
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: pnpm build
