name: Test
description: Run tests across the monorepo using pnpm and Vitest
inputs:
  working-directory:
    description: Directory to run the command in
    required: false
    default: "."
runs:
  using: composite
  steps:
    - name: Test
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: pnpm test
